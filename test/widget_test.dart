import 'package:flutter_test/flutter_test.dart';
import 'package:bilder_app/main.dart';

void main() {
  test('MyApp can be constructed', () {
    const app = MyApp();
    expect(app, isNotNull);
  });
}
